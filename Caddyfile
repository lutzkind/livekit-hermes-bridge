:80 {
    # Keep browser signaling same-origin and forward it directly to LiveKit.
    handle /rtc/* {
        reverse_proxy livekit-paob6cd3yz6t7cfdwnuahid1:7880
    }

    # Everything else is served by the FastAPI bridge.
    handle {
        reverse_proxy livekit-hermes-bridge:8000
    }
}
