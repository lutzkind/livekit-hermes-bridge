voice.46.224.200.122.sslip.io:8443 {
    # LiveKit WebSocket / API — proxy to LiveKit server via Traefik
    # Strips /livekit prefix so LiveKit receives /rtc?token=xxx
    handle_path /livekit* {
        reverse_proxy host.docker.internal:80 {
            header_up Host livekit-paob6cd3yz6t7cfdwnuahid1.46.224.200.122.sslip.io
        }
    }
    
    # Everything else goes to the bridge
    handle {
        reverse_proxy bridge:8000
    }
    
    tls internal
}
